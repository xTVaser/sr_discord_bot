module RunTracker
  module SrcAPI

    def self.getGameInfo(gameData)

      # if gameData.length > 1
      #   raise "Game ID Malformed, returning multiple games (this should be impossible)."
      # elsif gameData.has_key?('status')
      #   raise "Game ID Malformed, no game found with that ID: "
      #
      # TrackedGame.new(gameData['id'])

    end

  end
end
